name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"  # 每天 0 点 UTC 自动运行

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest

    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}

          ### 内容设置 ###
          SHOW_TITLE: true
          SECTION_NAME: waka  # 对应你的 README 中的占位符
          BLOCKS: ░▒▓█
          TIME_RANGE: last_7_days
          LANG_COUNT: 8
          SHOW_TIME: true
          SHOW_TOTAL: true
          STOP_AT_OTHER: true
          IGNORED_LANGUAGES: JSON TOML Binary

          ### 提交行为 ###
          COMMIT_MESSAGE: 📊 Update WakaTime stats
          TARGET_BRANCH: main
          TARGET_PATH: README.md
          COMMITTER_NAME: WakaTimeBot
          COMMITTER_EMAIL: bot@wakatime.dev
          AUTHOR_NAME: Fox-7th
          AUTHOR_EMAIL: fox@wakatime.dev
